<li class="items">
	{{this.dataValues.burger_name}}

	<button class="devour-burger btn waves-effect" data-id="{{this.dataValues.id}}" data-newdevoured="{{devour}}">
		{{#if devour}}devour{{else}}Re-order{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.dataValues.id}}">DELETE</button>
</li>
